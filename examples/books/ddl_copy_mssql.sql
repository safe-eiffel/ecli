create table COPY (
 isbn VARCHAR(14),
 serial_number INTEGER,
 purchased DATETIME,
 price FLOAT,
 loc_store INTEGER,
 loc_shelf INTEGER,
 loc_row INTEGER,
 borrower INTEGER,
 borrow_time DATETIME
)


Expérience - Query Assistant

Nouveaux besoins

* Associer un type à une colonne (result/parameter) et veiller à circonvenir la fabrique ou bien l'étendre pour qu'elle permette de créer un élément sur base du nom.
* Dans les squelettes on ne devrait pas trouver de prototype deferred, juste les do_...


? référencer/partager un result-set déjà nommé.
? référencer/partager un parameter-set déjà nommé
DONE * étendre un result-set ou un parameter-set
DONE * nommer le result-set
DONE * nommer le parameter-set


Nouvelles classes :

QA_PARAMETER_SET
QA_RESULT_SET

Shopping-list:

syntaxe XML proposée:

	<parameter ... type="ecli_type" ...>
	<result name="nom-colonne" type="ecli_type" ...>

syntaxe proposée : result_set, parameter_set

<result_set name="toto" [extends="lulu"]>
<parameter_set name ="toto" [extends="lulu"]>
	<parameter .... >
</parameter_set>